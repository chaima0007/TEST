# 📋 JOURNAL — TEST / Nexus-Market

> Snapshots §5. Règle anti-bruit : rien changé → une ligne « aucun changement ». Un événement réel = une entrée datée.

---

SNAPSHOT 2026-09-21 — **Réconciliation avec `main` : d'autres sessions avaient beaucoup avancé.** `main` était passé de `7b9552dd` à `cf2e105e` (**20 commits**) pendant que je travaillais sur ma branche. Constats, tous vérifiés : (1) **La convention de numérotation a été TRANCHÉE PAR CHAIMA le 2026-09-19** — `ERR-AAAAMMJJ-HHMM`, et ne jamais réutiliser le compteur séquentiel. Mes 12 entrées `ERR-022`→`ERR-032` la violaient et **collisionnaient sur `ERR-020` et `ERR-021`**, qui signifient autre chose sur `main`. Toutes renumérotées avec les **horodatages réels de mes commits** — aucune heure inventée. **81 références croisées** mises à jour. (2) **Doublon pur :** mon `ERR-028` décrivait la même erreur que leur `ERR-20260914-2033`, consignée **cinq jours avant**. Fusionnée : mes trois preuves propres reportées en note datée sous la leur, mon entrée devenue un renvoi (pas effacée — une entrée supprimée laisse des références orphelines). (3) **Doublon de script évité de justesse :** mon `verifier-registres.sh` refaisait le contrôle « ID en double » que leur `audit-codex.sh` faisait déjà. Retiré, et **frontière écrite en tête des deux fichiers**. (4) **Mes entrées ne suivaient pas leur format** (`- **Correctif`) : leur audit les comptait comme sans correctif. Conformées — s'y plier vaut mieux que défendre la sienne. (5) **ERR-20260914-2022 réellement fermée :** contrôle de **fraîcheur** ajouté à *leur* `audit-codex.sh` (le SHA cité dans `ETAT.md` comparé à `origin/main`), et `ETAT.md` remis à jour (`cf2e105e`, il annonçait encore `0c792ca3` du 14/09). (6) Vérifié sur `main` : les deux affirmations non étayées de `hermes.ts`/`pacte.ts` **sont corrigées** (PR #12), mais **`garde-fou.ts` est toujours absent de `main`** et `BANNED` n'est consulté que dans `LLMHermes`, qui se replie sur le chemin non contrôlé. Le trou structurel d'ERR-016 reste ouvert. **Union sans perte : 37 entrées** (24 + 32 − 19 communes). **Gate vert :** lint 0, build OK, tsc 0, 77/77, audit critique 0. Détection des deux scripts re-prouvée par test piégé.

SNAPSHOT 2026-09-20 11h35 UTC : **GO de Chaima — 8 correctifs parqués appliqués** (commit `870d45af`). **Vulnérabilités :** `next` → 16.3.5, `next-auth` → beta.32, `@tailwindcss/postcss` → 4.3.3 → **22 → 17, et 3 critiques → 0**. **Contrôle créé :** `scripts/verifier-registres.sh` (marqueurs de conflit · numéros en double · références orphelines · rétrécissement du registre · données personnelles), **détection prouvée dans les deux sens par test piégé**, branché en étape bloquante de la CI avec `fetch-depth: 0`. **Constat du jour : la CI ne lançait pas les tests** — le gate de `CLAUDE.md` les exigeait pourtant depuis toujours ; corrigé. **4 règles écrites dans `CLAUDE.md`** (proxy d'egress permanent · PR ouvertes au rituel §5 · un fait d'état à un seul endroit · « APPLIQUÉ » exige un SHA de `main`). **Gate vert avant push :** lint 0 erreur, `next build` OK, `tsc` 0, 77/77 tests, registres 5/5. **Reste nommément :** ERR-20260914-2025 (fichier sur une autre branche — je ne pousse pas hors de la branche de dev sans un mot explicite) · rétrogradation Prisma 7.8→6.19 (rupture majeure, §10) · dépôt en privé · ERR-016 (texte commercial). ERR-20260914-2022 laissée en **PARTIEL** : le contrôle de fraîcheur du SHA d'`ETAT.md` n'est pas construit, je ne le déclare pas corrigé.

SNAPSHOT 2026-09-20 11h20 UTC : **Rangement des projets — partie livrable exécutée.** (1) **Blocage confirmé deux fois** : `POST /user/repos` → `403 Resource not accessible by integration`. Cette session ne peut pas créer de dépôt. Les 16 dépôts (noms tranchés par Chaima : simples) doivent être créés par elle, vides. (2) **3 documents rangés au Drive** depuis des branches étrangères : audit PI/brevetabilité CompeteIQ → « Brevetabilité de nos projets » ; calcul des 29 h réelles Caelum + La Loi Avec Moi → « COMPILATION & SYNOPSIS » ; charte consolidée de veille → « Veille & Opportunités ». (3) **ERR-20260920-1123, ma faute, corrigée dans la minute** : j'ai déposé une **norme** au Drive alors que son auteur avait explicitement refusé de le faire le 2026-09-11 pour ne pas recréer la divergence d'E-18. Document renommé « COPIE QUI NE FAIT PAS FOI ». Les deux fichiers de veille restants n'iront pas au Drive : leur place est le dépôt Caelum. (4) **Trois corrections de classement**, toutes trouvées en ouvrant le contenu au lieu de lire les noms : `beautiful-hawking` n'est pas étrangère (c'est la cartographie de ce dépôt) → **19 étrangères, pas 20** ; `empire-chaima-linux-game` contient des **fiches de cours Linux/réseau**, pas un jeu ; `libre-accomplis-system` est un **projet complet** (28 fichiers, code exécutable), pas des documents. (5) **Deux faits établis** : La Loi Avec Moi a son dépôt, `chaima0007/droit-citoyen-app` ; et le refus 403 était **déjà consigné le 2026-09-11** dans une charte rangée sur une branche étrangère — huit jours perdus sur une contrainte déjà connue. Rien supprimé, rien mergé, rien envoyé (§10).

SNAPSHOT 2026-09-19 13h30 UTC : **Frontières entre projets + incident RGPD.** (1) À la demande de Chaima (« les projets se mélangent »), recomptage VÉRIFIÉ : **38 branches distantes, 20 appartenant à d'autres projets** (33 et ~16 le 2026-09-14 → **+5 en 5 jours**). Trois étrangères actives en 8 jours, dont ATLAS (`claude/nifty-shannon-u87dv8`) **ce jour**. Posé : `/codex/FRONTIERES.md`, agent `gardien-des-frontieres`, encadré « chacun reste à sa place » en tête des spécificités de `CLAUDE.md`. Rien déplacé, rien supprimé (§10) — aucune de ces branches n'est dans `main`, chacune est la seule copie de son travail. (2) **ERR-20260919-1324, CRITIQUE** : vérification via l'API GitHub → ce dépôt est **public** ; le commit `5be0a032` du matin y avait publié adresse du domicile, date de naissance et situation sociale. Caviardé le jour même (faits décisionnels conservés, valeurs non recopiées dans l'entrée d'erreur). **L'historique git les conserve : passer le dépôt en privé est une décision de Chaima.** (3) **ERR-20260919-1316** : sept entrées du registre (`ERR-020`→`ERR-20260914-2026`) restaurées après une disparition silencieuse en merge. Registre : 30 entrées contiguës, zéro référence orpheline. Rien d'engagé.

SNAPSHOT 2026-09-19 13h12 UTC : **Parcours 2 clos (dépôt C1+C1C) + 5 agents de sortie/clôture.** État réel vérifié : `origin/main` à `7b9552dd`, branche de dev `claude/charming-galileo-cqhkn1` à `4335ce29`, arbre propre avant travaux. (1) **Arbitrage rendu** sur le dépôt Tremplin : recommandation PROPOSÉ = ne pas déposer maintenant, déposer au premier de D1 (client payant identifié) ou D2 (premier acte d'exposition commerciale), 7 garde-fous, 3 faits NON VÉRIFIÉS bloquants. (2) **Le vérificateur-vérité a cassé une prémisse de l'arbitre avant qu'elle n'arrive à Chaima** : ERR-028 — `🔴 ERREURS.md` annonçait le garde-fou anti-survente « APPLIQUÉ le 2026-09-14 » alors que `lib/agents/garde-fou.ts` n'existe que sur une branche **non fusionnée** ; sur `main` les trois fuites commerciales sont vivantes (`hermes.ts:32`, `hermes.ts:75`, `pacte.ts:74`). (3) **Anti-doublon sur les 86 agents** des deux dépôts, puis création de 5 agents : `expediteur`, `percepteur`, `intendant-du-temps`, `relanceur-de-decisions`, `controleur-d-application`. Aucun agent existant touché. Rien d'engagé : aucun merge, aucun envoi, aucune dépense (§10).
SNAPSHOT 2026-09-19 (3) : **`main` = `8f17ae5a` — la PR #10 est MERGÉE.** Sept jours de travail en branche sont entrés sur la ligne principale, sur instruction explicite de Chaima (« ouvre et merge »), CI `Lint, Typecheck & Build` verte avant merge.
- **Vérification post-merge par l'agent `confirmateur`**, chaque ligne par commande exécutée : **5 artefacts sur 6 EN VIGUEUR** — règle du `git fetch`, règle du découpage, `scripts/audit-codex.sh`, les 3 agents de garantie, `codex/CARTOGRAPHIE.md`. Exactement **un ✗ est devenu ✓** : la règle du `git fetch` protège enfin les sessions.
- **La règle a servi dans l'heure qui a suivi sa mise en vigueur** : sans son `git fetch`, le confirmateur aurait raisonné sur `202a0016` et rendu un verdict faux sur 5 artefacts sur 6. Scénario ERR-011 / `ERR-20260914-1956`, évité par le garde-fou lui-même.
- **Défaut trouvé et corrigé** : l'entrée ERR-016 se contredisait — titre « NON CORRIGÉ », ligne Correctif « volet structurel **APPLIQUÉ** le 2026-09-14 », alors que `git cat-file -e origin/main:lib/agents/garde-fou.ts` échoue. Formulation remise d'aplomb en « **LIVRÉ, NON MERGÉ — donc PAS en vigueur sur `main`** », avec note datée. Rien du correctif n'est retiré ; seul le mot change. C'est exactement `ERR-20260914-2033`, trouvé cette fois par l'outil au lieu d'être subi.
- **Angle mort comblé** : les 5 artefacts tout juste mis en vigueur n'avaient **aucune ligne** dans la section 2 de l'audit — s'ils disparaissaient de `main`, l'audit resterait vert. Ajoutés, avec la règle d'extension écrite en commentaire. La surveillance passe de **6 à 11 artefacts**, 10 verts.
- **Fait mesuré sur ERR-016** : la branche porteuse a 4 commits en attente, **27 de retard**, et **aucune PR n'existe pour elle**. Ce n'est pas « prête à merger », c'est « à rendre mergeable d'abord » — et ça exige la décision commerciale.
- **ERR-003 vérifiée en direct** : le déploiement Vercel `laloiavecmoi` est passé en **Error** sur une PR strictement documentaire (11 fichiers, zéro ligne de code applicatif). Le coût de la ligne « Nettoyer les projets Vercel », qui dort depuis 64 jours, n'est pas théorique.
- VÉRIFIÉ : lint 0 erreur, build OK, tsc **0 après build**, **77/77 tests**.
- **Aucune branche supprimée. La branche du garde-fou n'a pas été mergée : ses 4 tests adverses sont rouges à dessein** (§10).

SNAPSHOT 2026-09-19 (2) : `main` = `202a0016` — **a avancé de 12 commits** pendant la session (PR #9, réconciliation des agents, convention de numérotation tranchée). **Trois agents de garantie créés, et essayés.**
- **`valideur` · `essayeur` · `confirmateur`** créés dans `.claude/agents/`. Anti-doublon §9 fait avant écriture : `verificateur-verite` source ce qui SORT, `testeur-adverse` ÉCRIT le test rouge, `superviseur-vigie` fait le snapshot — **aucun** ne vérifie qu'un correctif **tourne**. Chacun est justifié par des entrées du registre, nommées.
- **Essayés, et le résultat est un constat, pas un succès :** `Agent type 'valideur' not found`. Le registre des agents est **figé au démarrage de session** — un agent écrit maintenant n'est pas utilisable maintenant. « Écrit ≠ en vigueur » à deux niveaux : pas utilisable dans la session qui le crée, pas utilisable par les autres tant que `main` ne l'a pas. Consigné dans la carte, §9.
- **Mandats exécutés à la main pour les éprouver.** `valideur` : prémisse d'ERR-016 → `garde-fou.ts` trouvé sur une seule branche, absent de `main`, `BANNED` toujours en place. `confirmateur` : audit exécuté → 4 correctifs en vigueur sur 6.
- **L'audit a trouvé un défaut dans lui-même** : faux positif de doublon, sa regex tronquant `ERR-20260914-1956` à `ERR-20260914`. Corrigé en l'exécutant — ce pour quoi l'`essayeur` existe.
- **Convention de numérotation adoptée** (tranchée par Chaima le 2026-09-19) : mes trois entrées, en collision avec ERR-020/021 de `main` (**5e et 6e collision**), passent en horodatage UTC réel de leurs commits — `ERR-20260914-1956`, `ERR-20260914-2033`, `ERR-20260916-1413`. Les entrées de `main` ne sont pas renommées.
- **Deux cartographies fusionnées sans écrasement** : sections 1-6 de la version de `main` (PR ouvertes, projets hébergés, état des registres), 7-11 de la mienne (correctifs en vigueur, audit exécutable, machine, rangement, pièges).
- VÉRIFIÉ : lint 0 erreur, build OK, tsc **0 après build**, **77/77 tests**.
- **Rien mergé dans `main`, aucune branche supprimée, aucune PR ouverte** (§10).

SNAPSHOT 2026-09-19 13h40 UTC — **2 décisions appliquées sur 3 ; la 3e reste à Chaima.** État réel vérifié (`git fetch`, pas de mémoire) : `origin/main` à `7b9552dd`, **aucun nouveau commit depuis le 16/09** ; branche `claude/beautiful-hawking-4w1zlm` à `185b16bd`, `main` est bien son ancêtre → aucun conflit. PR #9 : `Lint, Typecheck & Build` **vert**, Netlify **vert**, Vercel/`laloiavecmoi` rouge = 🔴 ERR-003 (projet étranger, commenté une seule fois), aucun nouveau commentaire ni revue depuis le 14/09. **Appliqué :** (1) convention de numérotation de `🔴 ERREURS.md` → horodatage `ERR-AAAAMMJJ-HHMM` sans renommer `ERR-001..021` — écrite en tête du registre, consignée dans A-DECIDER ; (2) recette complète de test d'accès en écriture des Routines → `/codex/routines-acces-ecriture.md`, avec re-mesure du jour : `create_trigger` refuse **toujours** `connectors` (« not available for this organization »). **(3) Contradiction `.claude/agents/` tranchée par Chaima le jour même, en faveur du remplacement** — question posée explicitement (merger / fermer / merge humain), réponse « merger ». La décision du 2026-09-11 « laisser en l'état » est **supersédée** et consignée comme abandonnée avec sa date (§6) ; l'avertissement en tête de `codex/agents-correspondance.md` devient une résolution. Motif retenu : les ébauches n'avaient ni champ `tools:` ni socle commun — donc ni §3, ni §10, ni §13, ni §14. Les 6 experts de domaine du 16/09 sont conservés (ajout pur, noms distincts).
SNAPSHOT 2026-09-19 : état réel `main` = `7b9552dd` — **inchangé depuis le 2026-09-16**. Le travail d'audit attend toujours ton merge (§10). **Session consacrée à rendre la surveillance automatique plutôt qu'à la refaire à la main.**
- **`scripts/audit-codex.sh` créé** — le rituel §5 exécutable en ~5 s, lecture seule. Sept contrôles : état serveur après `fetch`, écart branches ↔ `main` (ERR-20260914-2022), chaque correctif testé comme réellement en vigueur (ERR-021), intégrité du registre, structure §12 + les 21 rôles, contradictions `CLAUDE.md`/`ETAT.md`, décisions dormantes > 14 j. Né d'ERR-020/021/022 : *un audit qu'on refait à la main est un audit qu'on oublie de refaire.*
- **Défaut du script corrigé avant livraison** : sa première version annonçait « 3679 commits en attente », chiffre gonflé par `claude/swarm-50-agent-architecture` (historique étranger de 3539 commits). Les branches divergeant de plus de 100 commits sont désormais exclues du total — **140** commits réellement en attente. Même faute que le premier audit des branches : compter du bruit comme du signal.
- **`codex/A-DECIDER.md` remis en conformité §6** : le fichier n'était **pas trié par ancienneté**. Trié, et les 3 lignes de plus de 14 jours marquées 🔴 avec leur ancienneté en clair (« Statut CompeteIQ » 80 j, « Vercel » 64 j, « ANTHROPIC_API_KEY » 64 j).
- **Ligne la plus ancienne close, preuve à l'appui** : « Merger la PR #1 », ouverte le 2026-06-18, attendait depuis **93 jours** alors que `main` porte `9cc15c2f Merge PR #1` depuis le 14 septembre. La décision était prise ; personne ne l'avait close. Déplacée en « Décisions tranchées ».
- **Audit des 6 nouveaux agents** (5 experts + orchestrateur, arrivés le 2026-09-16) : les 21 rôles du §1 sont intacts, chaque nouvel agent déclare ce dont il se distingue (§9 anti-doublon respecté). **Aucun défaut à signaler** — noté pour ne pas re-débattre.
- **`codex/CARTOGRAPHIE.md` mise à jour** : tableau de bord redaté, section de l'audit exécutable, les 27 agents, et la précision `debat` (skill) ↔ `orchestrateur` (agent) — deux mécanismes pour une même garantie, ce n'est pas un doublon mais il faut le savoir.
- VÉRIFIÉ : lint 0 erreur, build OK, tsc **0 après build**, **77/77 tests**.
- **Rien mergé dans `main`, aucune branche supprimée, aucune PR ouverte** (§10).

SNAPSHOT 2026-09-16 — **6 agents créés (5 experts de domaine + 1 orchestrateur).** État réel vérifié : `main` à `70e657b6`, inchangé depuis le 2026-09-14. Constat de départ : les 21 rôles du §1 sont des rôles de **gouvernance** (ils délibèrent) ; **aucun** ne possédait un domaine technique — alors que 5 des 19 erreurs consignées (ERR-001, 002, 003, 010, 016) relèvent de la CI, du déploiement ou de la couche agents. Domaines établis en lisant `package.json` et l'arborescence, pas de mémoire. Frontières écrites dans chaque charte pour éviter le recouvrement avec les rôles existants (`gardien-donnees` garde le RGPD, `sentinel-securite` garde les CVE, `conservateur-secrets` garde les fuites). **Quatre agents refusés** au titre du §9 anti-bloat, motif écrit. L'`orchestrateur` ne produit rien : il aiguille, et garantit que l'avocat et le contradicteur partent dans le **même message** (§8). Ce commit **revient sur la décision du 2026-09-11** (« laisser `.claude/agents/` en l'état »), qui portait sur le remplacement des 21 rôles, non sur l'ajout — revirement consigné dans A-DECIDER.

SNAPSHOT 2026-09-16 ~12h30 UTC — **CARTOGRAPHIE + audit de rangement** (demande de Chaima : « pourquoi rien ne change ? »). État réel vérifié (`git fetch`, `for-each-ref`, API GitHub), pas de mémoire. **Réponse au constat :** rien ne change parce que **rien ne merge** — `main` est figé depuis le 14/09 20h28, avec **6 PR ouvertes** (dont 3 vieilles de ~3 mois, et 3 empilées sur une base qui n'est pas `main`) et **38 références distantes**. Le goulot est le §10 : merger est humain. Ce n'est pas une panne, c'est le protocole qui fait son travail — mais le stock monte. **Produit :** `codex/CARTOGRAPHIE.md` (carte vivante, rôle cartographe §1). **Écarts signalés, non corrigés seuls (§5.5) :** les 4 dossiers `/codex/` (candidates, expertise, opportunites, licences-sortantes) existent mais sont **vides** — la boucle d'expertise du §4 n'a jamais tourné, donc « consulter /codex/expertise avant toute recherche » est aujourd'hui une consigne sans objet. **Écart corrigé :** 7 des 8 obstacles d'exploitation du 14/09 étaient introuvables depuis le registre → ERR-021 les indexe. **Vérifié bon :** 20 entrées ERR, 13 snapshots, 14 décisions en attente, 19 événements EVOLUTION, registre IP tenu ; chaîne `lint && tsc && test && build` verte (77/77).

SNAPSHOT 2026-09-14 21h40 CEST : **Branche de réconciliation des agents rafraîchie sur `main` (`c58a82ee`) — et CONTRADICTION signalée, non arbitrée.** État réel vérifié (`git fetch`), pas de mémoire. (1) Merge de `main` dans la branche : 3 conflits (`A-DECIDER`, `EVOLUTION`, `JOURNAL`) résolus **en union** — aucune ligne écrasée, les deux historiques conservés (leçon ERR-009). Les 21 agents n'ont pas conflicté : `main` ne les a pas touchés. (2) **Contradiction trouvée** : `main` consigne « `.claude/agents/` — laisser en l'état, décision reconfirmée après correction de la prémisse, TRANCHÉ PAR CHAIMA » (11/09) ; cette branche applique l'inverse sur feu vert explicite de Chaima donné en session (11/09 également). Deux décisions opposées, même jour, même signataire. **Non arbitré — c'est §10.** Ligne ouverte dans A-DECIDER ; `main` reste intact tant que la PR n'est pas mergée. (3) Gate re-passé et daté. (4) Rappel : 🔴 ERR-020 (accès en écriture des Routines) reste **non close**.

SNAPSHOT 2026-09-14 21h30 CEST : **Passe de documentation (Chaima en surveillance, aucune action de sa part).** État réel vérifié, `main` `0c792ca3`. (1) **Deux documents de reprise étaient faux** : `ETAT.md` et `00-LIRE-D-ABORD.md` annonçaient « PR #1 ouverte, pas encore mergée » alors qu'elle est mergée depuis `9cc15c2f` — un agent repartant de là aurait travaillé sur une branche close. Corrigés, avec `main` daté et la mention que les tests 77/77 datent du 2026-09-11 et n'ont pas été re-exécutés. (2) Ajouté `reports/2026-09-14-2130-notes-exploitation-sessions-distantes.md` : 8 obstacles d'infrastructure rencontrés aujourd'hui, messages d'erreur verbatim (conteneur éphémère, refus du classificateur et leurs 3 motifs distincts, clone `--depth 1` single-branch, `main` qui bouge en séance, HTTP 403 du proxy sur la suppression de ref, redirection test→TEST, identifiant MCP changeant). (3) A-DECIDER : ligne ouverte sur les ~16 branches de projets étrangers hébergées ici — aucune mergée, donc aucune à supprimer ; la question est de les déplacer. Rien d'engagé.

SNAPSHOT 2026-09-14 21h15 CEST : **Réunion de décision (7 agents) + préparation de la prospection.** État réel vérifié sur `main` `30c544fd`. (1) Réunion Parcours 2 sur la zone de prospection : question **écartée** — sans scraping le bassin est le réseau existant, la zone se constate après inventaire. Amendée sur 4 points par le vérificateur-vérité avant sortie. (2) **Quasi-incident évité (ERR-015)** : le garde-fou « écrire l'inventaire dans le fichier » invitait à committer des données personnelles sur un dépôt **public** ; `.gitignore` durci et vérifié par `git check-ignore`. (3) Produit : `reports/2026-09-14-2115-preparation-prospection.md` — modèle de fichier prospects (vide), mention d'information RGPD (brouillon), squelette de périmètre écrit de l'offre avec 7 `À DÉCIDER` non remplis (§10). (4) Deux lignes A-DECIDER ouvertes : conformité RGPD, bornage de l'offre. Rien d'engagé.

SNAPSHOT 2026-09-11 22h03 CEST : **Revue automatique des PR désactivée + test d'accès en écriture.** État réel vérifié via `git ls-remote`, pas de mémoire — `main` était à `9cc15c2f` (PR #1 mergée pendant la session). (1) Décisions de Chaima appliquées : revue auto des PR désactivée (ERR-010 close, `workflow_dispatch` seul, réactivation documentée en tête de workflow) ; agents `.claude/agents/` laissés en l'état ; CompeteIQ maintenu en pause. (2) Test d'accès en écriture depuis l'environnement CCR « Par défaut » : **concluant** (commit `c8560a0` poussé sur branche dédiée, jamais sur `main`, aucune PR). (3) **Deux écarts consignés, non résolus** : la branche de test `codex/test-ecriture-env-defaut` n'a PAS pu être supprimée (HTTP 403 du proxy d'egress sur la suppression de ref — non contourné, signalé) ; et l'attachement des dépôts reste à faire côté environnement « Par défaut » pour que les sessions programmées fonctionnent (le conteneur est éphémère). (4) Écart de méthode rattrapé : la première version de cette branche partait d'un `main` périmé (`733aea41`) et aurait écrasé `A-DECIDER`/`EVOLUTION`/`JOURNAL`/`ERREURS` réécrits entre-temps — branche reconstruite sur `9cc15c2f`, justification factuelle corrigée (le check échouait, il ne « passait pas au vert »).
SNAPSHOT 2026-09-11 22h35 CEST : **`.claude/agents/` réconciliés avec le set canonique** (TRANCHÉ PAR CHAIMA). Les 21 ébauches de `ae13c0b` (11-12 lignes, auto-déclarées « NON VÉRIFIÉ comme set canonique », sans `tools:` ni socle) sont remplacées par les 21 agents complets alignés sur `keywordmoneymaker@ab6d4dd` — socle commun **byte-identique** entre les deux dépôts (empreinte `582d8c2cd342`), 234 → 1149 lignes, mêmes 21 noms, aucun ajout ni suppression. 6 lignes recalées sur la réalité d'ici (chaîne `lint && tsc && test && build`, pièges ERR-006/ERR-008, priorité Nexus-Market/Caelum). Referme la ligne A-DECIDER du 2026-09-06. Détail : `/codex/agents-correspondance.md`.
**Faute de protocole signalée (§5) :** cette session avait d'abord travaillé sur une branche vieille de 19 commits sans vérifier `origin/main` — d'où une première livraison qui croyait créer les agents alors qu'ils existaient. Branche repartie de `origin/main`. Leçon : `git ls-remote` / `git fetch origin main` AVANT de conclure sur l'état réel, jamais les fichiers locaux seuls.
Consigné aussi : 🔴 ERR-011 (accès en écriture des Routines, non clos, décision d'accès humaine).

SNAPSHOT 2026-09-11 17h12 CEST : **Deux bases documentaires créées** (demande de Chaima) — `📒 PROPRIÉTÉ.md` (registre unique de propriété : noms/marques, code, contenus, antériorité, actifs externes ; chaque ligne sourcée/datée par git ou marquée « à vérifier / à déposer », §13 : usage d'un nom ≠ marque déposée) et `🔴 ERREURS.md` refondu au format structuré (10 erreurs réelles : passé/cause/détection/correctif + méta-leçons). Poussées sur la branche (`c4c95db`) ET déposées dans Drive « COMPILATION & SYNOPSIS ». Consultables par tous les agents.

SNAPSHOT 2026-09-11 14h51 CEST : **Problème résolu — PR #1 remise mergeable.** Le check-in horaire a détecté `mergeable_state: dirty` (conflit avec `main` : PR #2 `cbe82e0` avait atterri sur `main` avec CI + les mêmes correctifs lint que ma branche). Merge de `origin/main` dans la branche, 3 conflits résolus en union : `package.json` (garde `typecheck` de main + `test`/`postinstall` de la branche), `prisma/seed.ts` (version sans cast de main), `app/dashboard/settings/page.tsx` (approche refs de main, validée par sa CI). Résolution SUR la branche de dev — pas un merge dans `main` (§10). VÉRIFIÉ : 77/77 tests, lint 0 err, `tsc --noEmit` 0, `next build` OK. Merge `064e144` poussé → PR repassée `dirty` → `unstable` (seul rouge non-bloquant = `laloiavecmoi`, misconfig connue). Merge dans `main` = décision de Chaima.

SNAPSHOT 2026-09-11 14h45 CEST : état réel `9df5764`. **Avancée (hors Vercel, choix libre)** : agent **RELANCE** — relance de devis (suivi post-proposition), referme la boucle après PACTE. Séquence J+3 / J+7 (adaptée à l'objection prix/timing/confiance) / J+14 clôture polie. Route `POST /api/relance/draft` + page `/dashboard/relance` + sidebar « Relance » + registre (FLEET=14). Distinct de HERMES (relance froide) : proposition déjà chaude. Garde-fous : aucune fausse urgence, aucune remise inventée (§13). Heuristique + Claude optionnel. Envoi manuel (§10). VÉRIFIÉ : 77/77 tests, lint 0 err, tsc 0 (post-build), `next build` OK.

SNAPSHOT 2026-09-11 14h33 CEST : état réel `05c7666`. **Avancée (hors Vercel, choix libre)** : agent **BOUSSOLE** — qualification / triage de leads, le maillon manquant entre HERMES (prospection) et PACTE (devis). Route `POST /api/boussole/qualify` + page `/dashboard/qualification` + sidebar « Qualification » + registre (FLEET=13). **100 % déterministe, aucun LLM** (choix protocolaire §10/§13 : score de règles explicites justifié, pas de pourcentage, pas de certitude inventée) → aucun chemin NON VÉRIFIÉ. Recommande, ne décide pas (§10). 1 test adverse a attrapé un vrai défaut (question « budgétaire » vs regex `/budget/` → reformulée). VÉRIFIÉ : 67/67 tests, lint 0 err, tsc 0 (post-build), `next build` OK.

SNAPSHOT 2026-09-16 : état réel `main` = `70e657b6` — **inchangé depuis le 2026-09-14** (`git fetch` + `git rev-parse`, pas de mémoire). **Rien n'a changé sur `main` parce que rien n'y a été mergé.**
- **ERR-20260914-2022** consignée : les 8 commits de la session du 2026-09-14 vivent sur `codex/err-019-branche-perimee`, jamais mergée. Le merge est humain (§10) ; la faute de l'agent est d'avoir rapporté « poussé » sans dire « pas sur `main` ». Deuxième tour de la boucle d'ERR-021 — la leçon avait été écrite par l'agent lui-même, le même jour, dans le même fichier.
- **Audit systématique des correctifs** : chaque entrée du registre nommant un artefact testée contre `origin/main`. **5 sur 7 en vigueur** (ERR-001, 005, 010, 015, 018). **2 absents** : `lib/agents/garde-fou.ts` (ERR-021) et la règle du `git fetch` (ERR-20260914-2022). Conséquence mesurée : `git show origin/main:AGENTS.md | grep -c codex-regle-fetch` → **0**, le garde-fou ne protège aucune session.
- **`codex/CARTOGRAPHIE.md` créée** — carte vivante exigée par le §1 (rôle cartographe) et jusqu'ici **inexistante**. Elle affiche en tête l'écart `main` ↔ branches, la table « où écrit-on quoi », la machine (4 agents Caelum + CompeteIQ en pause) et les pièges.
- **Audit de cohérence §5** : `CLAUDE.md:304` (branche de dev = `claude/nexus-market-agents-63dlku`) contredit `ETAT.md:8` (PR #1 mergée, « n'est plus la branche de travail »). **Signalé, non corrigé seul.**
- VÉRIFIÉ : lint 0 erreur (3 warnings préexistants), tsc **0 après `next build`**, **77/77 tests**, build OK.
- **Rien mergé dans `main`, aucune branche supprimée, aucune PR ouverte** (§10).

SNAPSHOT 2026-09-14 20h45 CEST : état réel `70e657b6` sur `main` (vérifié par `git fetch`, pas de mémoire). **Session d'audit — deux erreurs consignées, deux garde-fous posés, aucun code produit.**
- **ERR-020** — la branche `codex/mesure-option3` de cette session avait été créée **sans `git fetch`**, depuis un `main` périmé de 12 commits. Récidive d'ERR-011, invisible **parce que** la leçon faisait partie des commits non fetchés. Branche assainie par `git merge origin/main` (retard 0). Affirmation initiale « aurait supprimé 457 lignes » **corrigée après mesure** : un merge conflit, il ne supprime pas — le risque réel est de décider en aveugle et d'écraser en réécrivant un fichier entier.
- **ERR-021** — ERR-018 annonçait le volet structurel d'ERR-016 « APPLIQUÉ », mais `lib/agents/garde-fou.ts` **n'est pas sur `main`** : il vit sur `codex/testeur-adverse-garde-fou-survente`, non mergée. La protection anti-survente n'existe donc pas sur la ligne principale. Gate exécuté sur cette branche : `3 failed | 78 passed (81)` — les 3 rouges sont les tests adverses volontairement rouges du volet commercial. « 78 tests verts » était une vérité partielle.
- **Garde-fou posé** : règle du `git fetch` d'ouverture écrite dans `AGENTS.md` (chargé à chaque session via `@AGENTS.md`), avec deux tests exécutables — créer la branche depuis `origin/main`, et vérifier `retard = 0` avant d'annoncer un succès.
- **Audit des 35 branches distantes** (`reports/2026-09-14-audit-branches-perimees.md`, lecture seule) : 26 périmées avec collision, mais une seule compte — `claude/nexus-market-agents-63dlku`, **branche de dev déclarée dans `CLAUDE.md`**, 12 commits de retard et en collision sur `A-DECIDER.md`, `EVOLUTION.md` (append-only) et ce JOURNAL. Non touchée : ce n'est pas ma branche.
- **Collision d'ID rencontrée en direct** : une session parallèle a créé son propre ERR-019 pendant que j'écrivais le mien. Détectée par le `fetch` **avant** le merge ; mes entrées renumérotées en 020/021, celle de `main` conservée intacte.
- VÉRIFIÉ sur `main` + mes changements : lint 0 erreur (3 warnings préexistants), tsc **0 après `next build`** (ERR-008), **77/77 tests**, `next build` OK.
- **Rien n'a été mergé dans `main`, aucune branche supprimée, aucune PR ouverte** (§10).

SNAPSHOT 2026-09-11 13h53 CEST : état réel `25dc63c`. **Avancée (hors Vercel, choix libre validé par Chaima)** : agent **PACTE** — rédacteur de devis / proposition commerciale (closing), suite de HERMES dans l'entonnoir. Route `POST /api/pacte/draft` (sans état) + page `/dashboard/devis` (formulaire lead → devis affiché + copiable) + entrée sidebar « Devis » + registre (FLEET=12). Honnêteté dure (§10/§13) : jamais de paiement en ligne promis, modalités « À CONFIRMER » (pas d'inscription légale ni Stripe). Les 3 tests adverses ont attrapé de vrais défauts (« 100 % responsive » et « aucun paiement en ligne » trippaient mes propres garde-fous → reformulés). VÉRIFIÉ : 55/55 tests, lint 0 err, tsc 0 (post-build), `next build` OK.

SNAPSHOT 2026-09-11 13h42 CEST : état réel `8391052` (poussé sur `claude/nexus-market-agents-63dlku`). **Avancée (hors Vercel, sur demande de Chaima)** : HERMES est désormais **branché dans l'app** — route API `POST /api/hermes/draft` (sans état, aucune persistance, aucun envoi) + page `/dashboard/prospection` (formulaire prospect → 4 brouillons copiables, garde-fou humain visible §10) + entrée sidebar « PROSPECTION (CAELUM) ». Vitest couvre aussi `app/**/__tests__` (+5 tests de route). VÉRIFIÉ : lint 0 err, tsc 0 (post-build), 44/44 tests, `next build` OK. Note Next 16 : `PageProps` (type global auto-généré) n'existe qu'après `next build` — tsc seul échoue tant que `.next/types` est absent ; c'est un artefact, pas un vrai défaut (cf. AGENTS.md).

SNAPSHOT 2026-09-11 13h31 CEST : état réel `ae13c0b` (local = remote), structure §12 en place (21 agents). **Avancée (hors Vercel, sur demande de Chaima)** : agent **HERMES** — rédacteur de brouillons de prospection LinkedIn (note de connexion + variante A/B + 1er message + relance) pour l'offre « site web premium » 500€. Envoi manuel uniquement, aucune automatisation/scraping (§3/§10). Heuristique + Claude optionnel (repli). `lib/agents/hermes.ts` + tests. VÉRIFIÉ : 39/39 tests, lint 0, tsc 0 sur hermes.

SNAPSHOT 2026-09-06 23h12 CEST : **Installation du PROTOCOLE CODEX (v2026-09-06)** dans le projet.
- CLAUDE.md : bloc protocole intégral collé en tête + spécificités projet (§15.4).
- Structure §12 créée : `/codex/{candidates,expertise,opportunites,licences-sortantes}`, `A-DECIDER.md`, `EVOLUTION.md`, `📋 JOURNAL.md`, `🔴 ERREURS.md`.
- `.claude/agents/` : 21 agents générés **dérivés du §1** (NON VÉRIFIÉ comme set canonique) + `.claude/skills/debat/`.
- État réel (git ls-remote) au moment du snapshot : `0d84e42` sur `claude/nexus-market-agents-63dlku`.
- Décision en attente consignée dans `/codex/A-DECIDER.md` : réconcilier les agents avec le set canonique de l'Empire s'il existe ; nettoyer les projets Vercel.
